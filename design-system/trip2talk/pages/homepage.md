# Homepage Page Overrides

> **PROJECT:** Trip2Talk
> **Generated:** 2026-06-02 21:27:39
> **Page Type:** Landing / Marketing

> ⚠️ **IMPORTANT:** Rules in this file **override** the Master file (`design-system/MASTER.md`).
> Only deviations from the Master are documented here. For all other rules, refer to the Master.

---

## Page-Specific Rules

### Layout Overrides

- **Max Width:** 1200px (standard)
- **Layout:** Full-width sections, centered content
- **Sections:** 1. Full-screen interactive element, 2. Guided product tour, 3. Key benefits revealed, 4. CTA after completion

### Spacing Overrides

- No overrides — use Master spacing

### Typography Overrides

- **Display:** Fraunces (`font-serif`) — cinematic headings, tracking-tight, max clamp 6rem
- **Body:** DM Sans (`font-sans`) + Sarabun for Thai
- **Eyebrow:** uppercase, letter-spacing 0.2em, gold `#c9a962`

### Color Overrides

- **Strategy:** Pure black base `#050505`, surfaces `#0f0f11` / `#16161a`, warm gold accent `#c9a962` (not white CTA)
- **Hero:** full-screen MP4 + `rgba(0,0,0,0.45)` overlay + bottom vignette
- **Avoid:** aurora mesh gradients, cream body backgrounds, generic AI card grids

### Component Overrides

- No overrides — use Master component specs

---

## Page-Specific Components

- No unique components for this page

---

## Recommendations

- Effects: Badge hover effects, metric pulse animations, certificate carousel, smooth stat reveal
- CTA Placement: After interaction complete + Skip option for impatient users
